import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import GratitudeLight from './GratitudeLight';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GratitudeLight />
  </React.StrictMode>
);
